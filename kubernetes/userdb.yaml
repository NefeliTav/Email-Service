apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: userdb
spec:
  selector:
    matchLabels:
      app: userdb
  serviceName: "userdb"
  replicas: 3
  template:
    metadata:
      labels:
        app: userdb
    spec:
      terminationGracePeriodSeconds: 10
      containers:
      - name: mongo
        image: mongo:5.0.0
        command:
        - mongod
        - "--bind_ip_all"
        - "--replSet"
        - rs0
        ports:
        - containerPort: 27017
        volumeMounts:
        - name: userdb-volume
          mountPath: /data/db
  volumeClaimTemplates:
  - metadata:
      name: userdb-volume
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 0.5Gi
---
apiVersion: v1
kind: Service
metadata:
  name: userdb
  labels:
    app: userdb
spec:
  ports:
  - name: userdb
    port: 27017
    targetPort: 27017
  clusterIP: None
  selector:
    app: userdb