services:
    antispam:
        build: "antispam"
        ports:
            - "5000:5000"
    emaildb:
        #build: "emaildb"
        image: mongo:latest
        ports:
            - "27017:27017"
        environment:
            - MONGO_INITDB_ROOT_USERNAME=emaildb_client
            - MONGO_INITDB_ROOT_PASSWORD=hiddenpassword
